runtime:
  plugins_path: "plugins/"
plugins:
  golang:
    module_type: go
    path: .
    tests:
      - input: tests/input.json
        expected: tests/expected.json
sources:
  socket_main:
    type: socket
    path: "/tmp/sidecar.sock"
sinks:
  blackhole:
    type: blackhole
dag:
  - from:
      kind: source
      name: socket_main
    to:
      - kind: plugin
        name: golang

  - from:
      kind: plugin
      name: golang
    to:
      - kind: sink
        name: blackhole